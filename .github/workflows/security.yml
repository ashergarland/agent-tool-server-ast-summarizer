name: Security

on:
  push:
    branches: [main]
  pull_request:
  schedule:
    - cron: '19 4 * * 1'

permissions:
  contents: read
  security-events: write
  packages: read

jobs:
  # agent-tool-platform workflow pin: d3415dd9a7b825ff15fb869236cffbc5ffce5d65
  capability-security:
    permissions:
      contents: read
      security-events: write
      packages: read
    uses: ashergarland/agent-tool-platform/.github/workflows/capability-security.yml@d3415dd9a7b825ff15fb869236cffbc5ffce5d65
